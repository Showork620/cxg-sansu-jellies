# ぷるぷるゼリー足し算ゲーム TODO

## Phase 0: プロジェクト準備

- [ ] Vite + React + TypeScript プロジェクトを作成する。
- [ ] GitHub Pages 用の `base` 設定方針を決める。
- [ ] ESLint / TypeScript / build コマンドを確認する。
- [ ] `src/styles/tokens.css` に色、余白、サイズ、z-indexのCSS変数を定義する。
- [ ] `100dvh` 対応とスマホ縦画面のベースレイアウトを作る。

## Phase 1: ゲーム基盤

- [ ] `src/game/types.ts` に基本型を定義する。
- [ ] `src/game/constants.ts` にレベル別設定、最大値、長押し時間などを定義する。
- [ ] `src/game/problemGenerator.ts` を作る。
- [ ] 足し算問題生成で `left + right <= 10` を保証する。
- [ ] 直近3問の完全一致を避ける。
- [ ] 将来用に引き算問題生成関数も作る。
- [ ] `src/game/choiceGenerator.ts` を作る。
- [ ] 4択に正解、範囲内、重複なし、ランダム順を保証する。
- [ ] `src/game/storage.ts` を作る。
- [ ] 設定保存、進捗保存、壊れた保存値の復旧を実装する。

## Phase 2: 初回体験

- [ ] `SplashScreen` を作る。
- [ ] タイトル、ゼリー、赤いキャラクターを表示する。
- [ ] `SetupScreen` を作る。
- [ ] モード選択UIを作る。
- [ ] レベル選択UIを作る。
- [ ] 音ON/OFF UIを作る。
- [ ] `このせっていではじめる` で設定を保存する。
- [ ] 初回設定後は通常ゲーム画面へ遷移する。

## Phase 3: 通常ゲーム画面

- [ ] `GameScreen` を作る。
- [ ] 上部中央に `Equation` を配置する。
- [ ] 中央に `JellyBoard` を配置する。
- [ ] 下部に `AnswerChoices` を配置する。
- [ ] 右下に `Character` を配置する。
- [ ] 左上に `ParentMenuHandle` を配置する。
- [ ] 閉じるボタンや戻るボタンが通常画面に出ないことを確認する。

## Phase 4: 数式と回答

- [ ] `Equation` で `3 + 1 = ?` を表示する。
- [ ] 正解時だけ `3 + 1 = 4` に切り替える。
- [ ] `AnswerChoices` で4つの大きな数字ボタンを表示する。
- [ ] 回答可能前は選択肢を disabled にする。
- [ ] 正解判定を実装する。
- [ ] 不正解時は選択ボタンを軽く揺らす。
- [ ] 不正解時に `answering` へ戻し、回答継続できるようにする。
- [ ] 正解時は正解ボタンを黄色でハイライトする。

## Phase 5: ゼリーブロック

- [ ] `JellyBlock` を作る。
- [ ] 角丸、半透明、光沢、柔らかい影をCSSで表現する。
- [ ] `jellyLayout.ts` に5列2段の配置計算を実装する。
- [ ] `JellyBoard` で左の青ゼリーと右の赤ゼリーを表示する。
- [ ] 集約エリアを表示する。
- [ ] 0個の場合の空表示を作る。
- [ ] レベル1で集約数を大きく表示する。
- [ ] レベル2で集約数を非表示にする。
- [ ] レベル3でゼリーを薄く表示し、操作不可にする。

## Phase 6: ドラッグ操作

- [ ] `useDragJellies` を作る。
- [ ] Pointer Events でドラッグ開始、移動、終了を扱う。
- [ ] ドラッグ中にゼリーを少し拡大する。
- [ ] ドロップ成功判定を実装する。
- [ ] 成功時に集約エリアへスナップする。
- [ ] 失敗時に元位置へ戻す。
- [ ] 右側ゼリーをすべて集約したら回答可能にする。
- [ ] `right === 0` のとき操作なしで回答可能にする。
- [ ] `prefers-reduced-motion` 時はアニメーションを弱める。

## Phase 7: キャラクター

- [ ] `Character` をReact SVGで作る。
- [ ] 赤い身体、ジグザグ頭、手足、長めの足を表現する。
- [ ] `idle` 表情を作る。
- [ ] `watching` 表情を作る。
- [ ] `thinking` 表情を作る。
- [ ] `confused` 表情を作る。
- [ ] `happy` 表情を作る。
- [ ] `excited` 表情を作る。
- [ ] ゲーム状態に応じてキャラ状態を切り替える。

## Phase 8: 正解演出

- [ ] `SuccessOverlay` を作る。
- [ ] `やったね！` を表示する。
- [ ] `つぎのもんだいへ` ボタンを表示する。
- [ ] 紙吹雪を少量表示する。
- [ ] ゼリーをぷるんと揃える。
- [ ] キャラをジャンプして喜ぶ状態にする。
- [ ] 次問題ボタンで新しい問題を生成する。
- [ ] 正解後に自動遷移しないことを確認する。

## Phase 9: 長押しメニュー

- [ ] `useLongPress` を作る。
- [ ] 1000ms以上の長押しでだけメニューを開く。
- [ ] 通常タップでは開かないようにする。
- [ ] pointer move / cancel / up で長押しをキャンセルする。
- [ ] `ParentMenu` を作る。
- [ ] モード選択を表示する。
- [ ] MVPでは引き算を準備中として扱う。
- [ ] レベル選択を表示する。
- [ ] 音ON/OFFを表示する。
- [ ] 進捗リセットを表示する。
- [ ] 進捗リセットには確認ステップを入れる。
- [ ] ゲームに戻る操作を実装する。
- [ ] メニュー変更後に設定をLocalStorageへ保存する。

## Phase 10: PWA / デプロイ

- [ ] `vite-plugin-pwa` を導入する。
- [ ] `manifest.webmanifest` 相当の設定を追加する。
- [ ] アプリ名、短縮名、テーマカラー、背景色を設定する。
- [ ] アイコンは仮アイコンで開始する。
- [ ] Service Worker のキャッシュ方針を設定する。
- [ ] GitHub Pages 用の build を通す。
- [ ] `npm run build` で成果物が生成されることを確認する。
- [ ] GitHub Pages の公開パスでリロードしても表示できることを確認する。

## Phase 11: テスト

- [ ] 問題生成の単体テストを追加する。
- [ ] 4択生成の単体テストを追加する。
- [ ] storage の単体テストを追加する。
- [ ] レベル別の回答可能条件をテストする。
- [ ] 375x667で手動確認する。
- [ ] 390x844で手動確認する。
- [ ] 430x932で手動確認する。
- [ ] 通常タップでメニューが開かないことを確認する。
- [ ] 長押しでメニューが開くことを確認する。
- [ ] 不正解後に続けて回答できることを確認する。
- [ ] 正解後に式が完成することを確認する。

## Phase 12: 後回し拡張

- [ ] 引き算モードの完全操作を実装する。
- [ ] 引き算レベル1の残数表示を実装する。
- [ ] サウンドを実装する。
- [ ] PWAアイコンを正式作成する。
- [ ] キャラクターアニメーションを細かくする。
- [ ] ゼリーのぷるぷる物理表現を強化する。
- [ ] 初回チュートリアルを必要に応じて追加する。
- [ ] 保護者向けの簡易進捗表示を追加する。

## MVP チェックリスト

- [ ] 足し算モードで問題が無限に続く。
- [ ] レベル1でゼリー操作と合計値表示がある。
- [ ] レベル2でゼリー操作があり、合計値は出ない。
- [ ] レベル3でゼリー操作ができない。
- [ ] 4択が正しく生成される。
- [ ] 正解で式が完成する。
- [ ] 不正解で責める演出にならない。
- [ ] 赤いキャラクターが右下に表示される。
- [ ] 長押しメニューが機能する。
- [ ] 設定がLocalStorageに保存される。
- [ ] GitHub Pages向けにbuildできる。
