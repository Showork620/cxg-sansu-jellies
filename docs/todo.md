# ぷるぷるゼリー足し算ゲーム TODO

## Phase 0: プロジェクト準備

- [ ] Vite + React + TypeScript プロジェクトを作成する。
- [ ] GitHub Pages 用の `base` 設定方針を決める。
- [ ] ESLint / TypeScript / build コマンドを確認する。
- [ ] `src/styles/tokens.css` に色、余白、サイズ、z-indexのCSS変数を定義する。
- [ ] `100dvh` 対応とスマホ縦画面のベースレイアウトを作る。

## Phase 1: ゲーム基盤

- [ ] `src/game/types.ts` に基本型を定義する。
- [ ] `src/game/constants.ts` にレベル別設定、最大値、長押し時間などを定義する。
- [ ] `src/game/problemGenerator.ts` を作る。
- [ ] 足し算問題生成で `left + right <= 10` を保証する。
- [ ] 直近3問の完全一致を避ける。
- [ ] 将来用に引き算問題生成関数も作る。
- [ ] `src/game/choiceGenerator.ts` を作る。
- [ ] 4択に正解、範囲内、重複なし、ランダム順を保証する。
- [ ] `src/game/storage.ts` を作る。
- [ ] 設定保存、進捗保存、壊れた保存値の復旧を実装する。
- [ ] `src/audio/soundManifest.ts` を作る。
- [ ] `src/audio/soundManager.ts` を作る。
- [ ] `src/assets/sounds/README.md` に音源ライセンス記録フォーマットを作る。
- [ ] `src/feedback/feedbackTypes.ts` を作る。
- [ ] `src/feedback/visualFeedback.ts` を作る。
- [ ] `src/feedback/haptics.ts` を作る。
- [ ] `src/feedback/feedbackManager.ts` を作る。

## Phase 2: 初回体験

- [ ] `SplashScreen` を作る。
- [ ] タイトル、ゼリー、赤いキャラクターを表示する。
- [ ] `SetupScreen` を作る。
- [ ] モード選択UIを作る。
- [ ] レベル選択UIを作る。
- [ ] 音ON/OFF UIを作る。
- [ ] ブルブルON/OFF UIを作る。
- [ ] `このせっていではじめる` で設定を保存する。
- [ ] 初回設定後は通常ゲーム画面へ遷移する。

## Phase 3: 通常ゲーム画面

- [ ] `GameScreen` を作る。
- [ ] 上部中央に `Equation` を配置する。
- [ ] 中央に `JellyBoard` を配置する。
- [ ] 下部に `AnswerChoices` を配置する。
- [ ] 右下に `Character` を配置する。
- [ ] 左上に `ParentMenuHandle` を配置する。
- [ ] 閉じるボタンや戻るボタンが通常画面に出ないことを確認する。

## Phase 4: 数式と回答

- [ ] `Equation` で `3 + 1 = ?` を表示する。
- [ ] 正解時だけ `3 + 1 = 4` に切り替える。
- [ ] `AnswerChoices` で4つの大きな数字ボタンを表示する。
- [ ] 回答可能前は選択肢を disabled にする。
- [ ] 正解判定を実装する。
- [ ] 不正解時は選択ボタンを軽く揺らす。
- [ ] 不正解時に `answering` へ戻し、回答継続できるようにする。
- [ ] 正解時は正解ボタンを黄色でハイライトする。

## Phase 5: ゼリーブロック

- [ ] `JellyBlock` を作る。
- [ ] 角丸、半透明、光沢、柔らかい影をCSSで表現する。
- [ ] `jellyLayout.ts` に5列2段の配置計算を実装する。
- [ ] `JellyBoard` で左の青ゼリーと右の赤ゼリーを表示する。
- [ ] 集約エリアを表示する。
- [ ] 0個の場合の空表示を作る。
- [ ] レベル1で集約数を大きく表示する。
- [ ] レベル2で集約数を非表示にする。
- [ ] レベル3でゼリーを薄く表示し、操作不可にする。

## Phase 6: ドラッグ操作

- [ ] `useDragJellies` を作る。
- [ ] Pointer Events でドラッグ開始、移動、終了を扱う。
- [ ] ゼリー操作エリアに限定して `touch-action: none` を設定する。
- [ ] `setPointerCapture` / `releasePointerCapture` を使ってドラッグ中の追従を安定させる。
- [ ] ドラッグ中にゼリーを少し拡大する。
- [ ] ドラッグ中の速度から最大±8degの傾きを計算する。
- [ ] ドラッグ中の表示座標を少し遅延追従させ、最大遅れを12pxに制限する。
- [ ] ドロップ成功判定を実装する。
- [ ] 成功時に集約エリアへスナップする。
- [ ] 成功時に縦潰れ、跳ね、着地のぷるん演出を入れる。
- [ ] 失敗時に元位置へ戻す。
- [ ] 失敗時に短い横揺れと戻り演出を入れる。
- [ ] 右側ゼリーをすべて集約したら回答可能にする。
- [ ] `right === 0` のとき操作なしで回答可能にする。
- [ ] `prefers-reduced-motion` 時はアニメーションを弱める。

## Phase 7: キャラクター

- [ ] `Character` をReact SVGで作る。
- [ ] 赤い身体、ジグザグ頭、手足、長めの足を表現する。
- [ ] `idle` 表情を作る。
- [ ] `watching` 表情を作る。
- [ ] `thinking` 表情を作る。
- [ ] `confused` 表情を作る。
- [ ] `happy` 表情を作る。
- [ ] `excited` 表情を作る。
- [ ] ゲーム状態に応じてキャラ状態を切り替える。

## Phase 8: 正解演出

- [ ] `SuccessOverlay` を作る。
- [ ] `やったね！` を表示する。
- [ ] `つぎのもんだいへ` ボタンを表示する。
- [ ] 紙吹雪を少量表示する。
- [ ] ゼリーをぷるんと揃える。
- [ ] キャラをジャンプして喜ぶ状態にする。
- [ ] 次問題ボタンで新しい問題を生成する。
- [ ] 正解後に自動遷移しないことを確認する。

## Phase 9: 視覚フィードバック / ハプティクス

- [ ] `FeedbackEventId` を定義する。
- [ ] `HapticPatternId` を定義する。
- [ ] `playFeedback(eventId)` から視覚、音、振動を同期して呼び出す。
- [ ] `jellyPress` でゼリーが軽く潰れる演出を実装する。
- [ ] `jellyDragStart` で拡大、光沢強調、掴んだ手触りを実装する。
- [ ] `jellyDragMove` で速度に応じた傾きと伸びを実装する。
- [ ] `jellyDropSuccess` でスナップ、跳ね、ぷるんを実装する。
- [ ] `jellyDropMiss` で戻りと横揺れを実装する。
- [ ] `jellyMergeComplete` で集約エリア全体の短い弾みを実装する。
- [ ] `answerCorrect` で式、ゼリー、ボタン、キャラの演出タイミングを合わせる。
- [ ] `navigator.vibrate` 対応チェックを実装する。
- [ ] `tap`, `softTick`, `softImpact`, `success`, `wrong` の振動パターンを定義する。
- [ ] ハプティクスの最小再生間隔を実装する。
- [ ] `document.visibilityState !== "visible"` のとき振動しないようにする。
- [ ] `settings.hapticsEnabled` を追加しLocalStorageに保存する。
- [ ] 保護者メニューに `ブルブル ON/OFF` を追加する。
- [ ] 非対応端末で例外が出ないことを確認する。
- [ ] `prefers-reduced-motion` 時に視覚演出と振動を弱める。

## Phase 10: サウンド

- [ ] 無料SE候補を選定する。
- [ ] CC0またはクレジット不要の素材を優先して採用する。
- [ ] 採用した音源の配布元、URL、ライセンス、取得日を `src/assets/sounds/README.md` に記録する。
- [ ] `jellyGrab` の音源を追加する。
- [ ] `jellyDrop` の音源を追加する。
- [ ] `jellySnap` の音源を追加する。
- [ ] `answerWrong` の音源を追加する。
- [ ] `answerCorrect` の音源を追加する。
- [ ] `successFanfare` の音源を追加する。
- [ ] `uiTap` の音源を追加する。
- [ ] `menuOpen` の音源を追加する。
- [ ] 初回ユーザー操作で音声をアンロックする。
- [ ] 音ON/OFFをSoundManagerへ反映する。
- [ ] SoundIdごとの音量を調整する。
- [ ] 同じSEが連打で重なりすぎないよう最小再生間隔を入れる。
- [ ] 音源ロード失敗時にゲームが止まらないようにする。
- [ ] 掴んだ瞬間に `jellyGrab` を鳴らす。
- [ ] ドロップ失敗時に `jellyDrop` を鳴らす。
- [ ] 集約エリアに置けたときに `jellySnap` を鳴らす。
- [ ] 不正解時に `answerWrong` を鳴らす。
- [ ] 正解時に `answerCorrect` と `successFanfare` を自然な間隔で鳴らす。
- [ ] 次問題へ進むときに `uiTap` または `nextProblem` を鳴らす。

## Phase 11: 長押しメニュー

- [ ] `useLongPress` を作る。
- [ ] 1000ms以上の長押しでだけメニューを開く。
- [ ] 通常タップでは開かないようにする。
- [ ] pointer move / cancel / up で長押しをキャンセルする。
- [ ] `ParentMenu` を作る。
- [ ] モード選択を表示する。
- [ ] MVPでは引き算を準備中として扱う。
- [ ] レベル選択を表示する。
- [ ] 音ON/OFFを表示する。
- [ ] ブルブルON/OFFを表示する。
- [ ] 進捗リセットを表示する。
- [ ] 進捗リセットには確認ステップを入れる。
- [ ] ゲームに戻る操作を実装する。
- [ ] メニュー変更後に設定をLocalStorageへ保存する。

## Phase 12: PWA / デプロイ

- [ ] `vite-plugin-pwa` を導入する。
- [ ] `manifest.webmanifest` 相当の設定を追加する。
- [ ] アプリ名、短縮名、テーマカラー、背景色を設定する。
- [ ] アイコンは仮アイコンで開始する。
- [ ] Service Worker のキャッシュ方針を設定する。
- [ ] GitHub Pages 用の build を通す。
- [ ] `npm run build` で成果物が生成されることを確認する。
- [ ] GitHub Pages の公開パスでリロードしても表示できることを確認する。

## Phase 13: テスト

- [ ] 問題生成の単体テストを追加する。
- [ ] 4択生成の単体テストを追加する。
- [ ] storage の単体テストを追加する。
- [ ] SoundManagerの音OFF、最小再生間隔、ロード失敗の単体テストを追加する。
- [ ] Hapticsの未対応端末、OFF設定、最小再生間隔の単体テストを追加する。
- [ ] FeedbackManagerのイベントディスパッチテストを追加する。
- [ ] レベル別の回答可能条件をテストする。
- [ ] 375x667で手動確認する。
- [ ] 390x844で手動確認する。
- [ ] 430x932で手動確認する。
- [ ] 通常タップでメニューが開かないことを確認する。
- [ ] 長押しでメニューが開くことを確認する。
- [ ] 音ON/OFFが即反映されることを確認する。
- [ ] 掴む、置く、正解、不正解のSEがタイミングよく鳴ることを確認する。
- [ ] ゼリーの掴む、移動、置く、戻る演出が自然なことを確認する。
- [ ] Android実機でハプティクスが控えめに鳴ることを確認する。
- [ ] iOS実機や非対応ブラウザでハプティクス未対応でも壊れないことを確認する。
- [ ] ブルブルON/OFFが即反映されることを確認する。
- [ ] `prefers-reduced-motion` で動きと振動が抑制されることを確認する。
- [ ] 不正解後に続けて回答できることを確認する。
- [ ] 正解後に式が完成することを確認する。

## Phase 14: 後回し拡張

- [ ] 引き算モードの完全操作を実装する。
- [ ] 引き算レベル1の残数表示を実装する。
- [ ] 追加サウンドバリエーションを実装する。
- [ ] Motion for Reactなどのspringライブラリ導入を検討する。
- [ ] Androidネイティブアプリ化する場合は `HapticFeedbackConstants` や `VibrationEffect` を検討する。
- [ ] PWAアイコンを正式作成する。
- [ ] キャラクターアニメーションを細かくする。
- [ ] ゼリーのぷるぷる物理表現を強化する。
- [ ] 初回チュートリアルを必要に応じて追加する。
- [ ] 保護者向けの簡易進捗表示を追加する。

## MVP チェックリスト

- [ ] 足し算モードで問題が無限に続く。
- [ ] レベル1でゼリー操作と合計値表示がある。
- [ ] レベル2でゼリー操作があり、合計値は出ない。
- [ ] レベル3でゼリー操作ができない。
- [ ] 4択が正しく生成される。
- [ ] 正解で式が完成する。
- [ ] 不正解で責める演出にならない。
- [ ] 赤いキャラクターが右下に表示される。
- [ ] ゼリーの掴む、移動、置く、戻るのぷるん演出がある。
- [ ] 対応端末で基本ハプティクスが鳴り、非対応端末でも安全に動く。
- [ ] 掴む、置く、正解、不正解、メニュー操作の基本SEが鳴る。
- [ ] 音ON/OFFが保存され、即反映される。
- [ ] ブルブルON/OFFが保存され、即反映される。
- [ ] 長押しメニューが機能する。
- [ ] 設定がLocalStorageに保存される。
- [ ] GitHub Pages向けにbuildできる。
